 Metode  Endepunkt      Beskrivelse                Body (JSON)       

 GET     `/api/movies`  Henter alle filmer         -                 
 POST    `/api/movies`  Legger til en ny film      `{ "title": "..." }` 
 DELETE  `/api/movies`  Sletter en film (Scaffold) -                 